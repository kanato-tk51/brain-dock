# 今日から始める具体的TODO

1. `schemas/sql/001_core.sql` を適用してローカルDBを作成する
2. CLIコマンド `capture` と `search` の最小実装Issueを起票する
3. `.github/labels.yml` をラベル同期してProject運用を開始する
4. 毎日使う入力フォーマット（1行）を固定する
5. `captures_raw` から `entries` を作る最小パーサを実装する
6. `recall` コマンド（会話用3行カード）を実装する
7. pre-commit にシークレット/PII検査を追加する
8. 週次レビューIssueを定期作成する運用を決める
9. `design/adr/0000-adr-template.md` で最初のADRを書く
10. 1週間運用して入力摩擦を測り、Phase 1を微修正する
