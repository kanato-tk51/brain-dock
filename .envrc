# brain-dock local runtime settings
VENV_PATH="$PWD/.venv"

if [ ! -x "$VENV_PATH/bin/python" ]; then
  python3 -m venv "$VENV_PATH"
fi

PATH_add "$VENV_PATH/bin"
export VIRTUAL_ENV="$VENV_PATH"
unset PYTHONHOME

dotenv_if_exists .env
export PYTHONDONTWRITEBYTECODE=1
